name: CSV Lint on CSV File Changes

on:
  push:
    paths:
      - "data/raw/**.csv"
      - ".github/workflows/lint.yml"
  pull_request:
    paths:
      - "**.py"
      - ".github/workflows/lint.yml"

jobs:
  csv-lint:
    runs-on: ubuntu-latest
    permissions:
      contents: read # Read access to the repository contents is enough for linting
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
        with:
          fetch-depth: 1
          persist-credentials: false

      - name: csvlinter
        uses: kcheriyath/csvlinter@def8e270b407a64b6cd1fa5862127ac62e3decd4 # V0.7.0
        with:
          find_pattern: "*.csv"
          fail_on_error: "true"
          extra_params: "--lazyquotes"
