"""NCHS Data Query System (DQS) collectors.

DQS is CDC/NCHS's unified query layer over the "Health, United States" report
family (NHANES, NHIS, NHAMCS, NVSS, NPALS, NHCS). Every topic is published as a
Socrata dataset on data.cdc.gov with a shared tidy schema — see pulse-code's
`pulse source dqs` for the full 28-dataset catalog. This package archives the
slices that feed health-charts.
"""
